# Ultralytics pose dataset descriptor. Paths are relative to this file.
path: ../data/pallet_pose
train: images/train
val: images/val
test: images/test
kpt_shape: [4, 3]
flip_idx: [1, 0, 3, 2]
names:
  0: pallet

